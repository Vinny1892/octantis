"""Built-in plugins shipped with Octantis core.

Each module here defines one adapter that exposes an existing core component
through the SDK Protocol surface and registers via an entry point in
`pyproject.toml`. The adapters are thin — all business logic stays in the
original module.
"""
